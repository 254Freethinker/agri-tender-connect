import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";

const FarmInputMarketplace = () => {
  return (
    <div className="container mx-auto px-4 py-8">
      <Card>
        <CardHeader>
          <CardTitle>Farm Input Marketplace</CardTitle>
          <CardDescription>
            Browse and purchase farm inputs from verified suppliers
          </CardDescription>
        </CardHeader>
        <CardContent>
          <p>This feature is under development. Check back soon!</p>
        </CardContent>
      </Card>
    </div>
  );
};

export default FarmInputMarketplace;