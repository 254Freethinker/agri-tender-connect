<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Base configuration with secure defaults -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
            <!-- Removed user certificates for production security -->
        </trust-anchors>
    </base-config>

    <!-- Debug configuration for development only -->
    <debug-overrides>
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>

    <!-- Domain-specific configuration for Supabase -->
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">cwcduhvwkihpnuaoflps.supabase.co</domain>
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </domain-config>
</network-security-config>
